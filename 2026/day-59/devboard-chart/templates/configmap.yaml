apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "devboard.fullname" . }}-config
  labels:
    {{- include "devboard.labels" . | nindent 4 }}
data:
  LOG_LEVEL: {{ .Values.config.logLevel | quote }}
